name: paid-datanet-quote-cli-v1

on:
  pull_request:
    paths:
      - "scripts/paid_datanet_quote_cli_v1.ts"
      - "scripts/prove_paid_datanet_quote_cli_v1.ts"
      - "docs/operators/paid-datanet-quote-cli-v1.md"
      - ".github/workflows/paid-datanet-quote-cli-v1.yml"
  push:
    branches:
      - main
    paths:
      - "scripts/paid_datanet_quote_cli_v1.ts"
      - "scripts/prove_paid_datanet_quote_cli_v1.ts"
      - "docs/operators/paid-datanet-quote-cli-v1.md"
      - ".github/workflows/paid-datanet-quote-cli-v1.yml"

permissions:
  contents: read

jobs:
  prove:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Prove quote CLI
        run: >-
          npx --no-install tsx
          scripts/prove_paid_datanet_quote_cli_v1.ts
