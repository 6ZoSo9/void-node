name: VOID WC/VOID vault deployer gas signing authorization v1

on:
  pull_request:
    paths:
      - ".github/workflows/void-wc-void-vault-deployer-gas-signing-authorization-v1.yml"
      - "ops/mainnet0/wc-void-market-vault-deployer-gas-signing-authorization-v1.json"
      - "tools/void-wc-void-market-vault-deployer-gas-signing-authorization-v1.mjs"
      - "scripts/prove_void_wc_void_market_vault_deployer_gas-signing-authorization-v1.mjs"
      - "docs/operators/wc-void-market-vault-deployer-gas-signing-authorization-v1.md"
      - "ops/mainnet0/wc-void-market-vault-deployer-gas-exact-signing-request-v1.json"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: void-wc-void-vault-deployer-gas-signing-authorization-v1-${{ github.ref }}
  cancel-in-progress: true

jobs:
  prove:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        node: [22, 24, 26]

    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          persist-credentials: false
          fetch-depth: 1

      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38
        with:
          node-version: ${{ matrix.node }}

      - run: npm ci --ignore-scripts

      - name: Prove exact one-signature authorization
        run: node scripts/prove_void_wc_void_market_vault_deployer_gas_signing_authorization_v1.mjs

      - name: Prove sign-once wrapper has no broadcast path
        run: node scripts/prove_void_wc_void_market_vault_deployer_gas_fixed_credential_sign_once_v1.mjs

      - name: Preserve exact signing request
        run: node scripts/prove_void_wc_void_market_vault_deployer_gas_exact_signing_request_v1.mjs

      - name: Diff hygiene
        run: git diff --check
