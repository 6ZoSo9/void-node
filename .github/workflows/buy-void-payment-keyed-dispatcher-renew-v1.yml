name: Buy VOID payment-keyed dispatcher renew v1

on:
  pull_request:
    paths:
      - ".github/workflows/buy-void-payment-keyed-dispatcher-renew-v1.yml"
      - "src/economic/buy_void_payment_keyed_dispatcher_renew_v1.ts"
      - "scripts/prove_buy_void_payment_keyed_dispatcher_renew_v1.ts"
      - "docs/architecture/buy-void-payment-keyed-dispatcher-renew-v1.md"
  push:
    branches: [main]
    paths:
      - ".github/workflows/buy-void-payment-keyed-dispatcher-renew-v1.yml"
      - "src/economic/buy_void_payment_keyed_dispatcher_renew_v1.ts"
      - "scripts/prove_buy_void_payment_keyed_dispatcher_renew_v1.ts"
      - "docs/architecture/buy-void-payment-keyed-dispatcher-renew-v1.md"

permissions:
  contents: read

jobs:
  prove:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    strategy:
      fail-fast: false
      matrix:
        node: [22, 24, 26]
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
          fetch-depth: 1
      - uses: actions/setup-node@v6
        with:
          node-version: \${{ matrix.node }}
      - run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Typecheck, build, and prove fixed-policy lease renewal
        run: |
          set -euo pipefail
          npm run typecheck
          npm run build
          node --import tsx scripts/prove_buy_void_payment_keyed_dispatcher_renew_v1.ts
          node --import tsx scripts/prove_buy_void_payment_keyed_dispatcher_runtime_preview_v1.ts
          node --import tsx scripts/prove_buy_void_payment_keyed_dispatcher_lease_context_v1.ts
          node --import tsx scripts/prove_buy_void_payment_keyed_dispatcher_claim_v1.ts
          node --import tsx scripts/prove_buy_void_payment_keyed_dispatcher_v1.ts
          ./node_modules/.bin/tsc \
            --noEmit \
            --target ES2022 \
            --module NodeNext \
            --moduleResolution NodeNext \
            --lib ES2022 \
            --types node \
            --esModuleInterop \
            --allowSyntheticDefaultImports \
            --downlevelIteration \
            --skipLibCheck \
            scripts/prove_buy_void_*.ts
          git diff --check
