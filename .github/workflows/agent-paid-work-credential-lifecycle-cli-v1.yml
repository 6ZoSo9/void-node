name: Agent paid-work credential lifecycle CLI V1

on:
  pull_request:
    paths:
      - ".github/workflows/agent-paid-work-credential-lifecycle-cli-v1.yml"
      - "docs/operators/agent-paid-work-credential-lifecycle-cli-v1.md"
      - "scripts/agent_paid_work_credential_lifecycle_cli_v1.ts"
      - "scripts/prove_agent_paid_work_credential_lifecycle_cli_v1.ts"
      - "scripts/agent_paid_work_credential_registry_v1.ts"
      - "scripts/agent_paid_work_order_envelope_v1.ts"
      - "package-lock.json"
      - "package.json"
      - "tsconfig.json"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prove:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - run: npm ci

      - name: Prove credential lifecycle CLI
        run: npx tsx scripts/prove_agent_paid_work_credential_lifecycle_cli_v1.ts

      - name: Regress credential registry
        run: npx tsx scripts/prove_agent_paid_work_credential_registry_v1.ts

      - name: Build
        run: npm run build
