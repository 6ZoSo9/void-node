[Service]
# Clear ANY previously accumulated ExecStartPre lines
ExecStartPre=
# Require the mount and the new key path
ExecStartPre=/usr/bin/mountpoint -q /mnt/voidkey
ExecStartPre=/usr/bin/test -r /mnt/voidkey/keys/nodeA.key
