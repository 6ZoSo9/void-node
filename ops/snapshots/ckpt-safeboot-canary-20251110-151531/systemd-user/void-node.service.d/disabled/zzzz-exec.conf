[Service]
ExecStart=
# Keep proxy preloader but run the app via the stable tsx launcher
ExecStart=/usr/bin/env -S bash -lc '\
  export HTTP_PORT=4102 BACKEND_HTTP_PORT=4102 READY_ULTRA_LOG=1 ; \
  NODE_OPTIONS="-r /home/zoso/.config/void/boot/ultra-ready.v3c.js" \
  npx --yes tsx src/index.ts'
