[Service]
# refuse to start if someone sets HEAD_TXT_SOURCE=api
ExecStartPre=/bin/sh -lc 'env | grep -q "^HEAD_TXT_SOURCE=api$" && { echo "[guard] HEAD_TXT_SOURCE=api forbidden"; exit 1; } || exit 0'
