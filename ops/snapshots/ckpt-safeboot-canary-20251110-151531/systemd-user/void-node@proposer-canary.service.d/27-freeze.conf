[Service]
Environment=PROPOSER_AUTO=0
Environment=VOID_PROPOSER_AUTOSTART=0
# refuse to bind 4100 ever
ExecStartPre=/bin/bash -lc 'test "${HTTP_PORT:-4110}" != "4100" || { echo "[guard] HTTP_PORT=4100 blocked"; exit 1; }'
