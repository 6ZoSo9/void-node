[Service]
ExecStartPost=/bin/bash -lc '\
  for i in {1..30}; do \
    ss -ltnH | grep -q ":4102 " && exit 0; \
    sleep 0.2; \
  done; \
  echo "[assert] 4102 never bound; dumping last 150 lines"; \
  journalctl --user -u void-node -n 150 -o cat; \
  exit 1'
