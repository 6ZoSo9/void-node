# Inactive example. Replace only through a separately authorized deployment.
# Use one exact Tailscale-reachable Precision SSH target.
VOID_PUBLIC_AGENT_FORWARD_TARGET=zoso@REPLACE_WITH_PRECISION_TAILSCALE_IP
