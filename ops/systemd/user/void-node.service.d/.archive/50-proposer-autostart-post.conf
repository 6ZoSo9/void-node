[Service]
# Start proposer auto-loop once HTTP is listening
ExecStartPost=/usr/bin/bash -lc 'for i in {1..40}; do curl -fsS 127.0.0.1:4100/proposer/auto/start?ms=2000 && exit 0; sleep 0.5; done; exit 1'
