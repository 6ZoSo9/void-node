{
  "$id": "https://void.invalid/schemas/authenticated-paid-work-runtime-disabled-production-activation-prerequisites-v1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "enabled": {
      "type": "boolean"
    },
    "expected": {
      "additionalProperties": false,
      "properties": {
        "execution_receipt_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "final_seal_receipt_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "install_checkpoint_tag": {
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{2,179}$",
          "type": "string"
        },
        "install_checkpoint_target": {
          "pattern": "^[0-9a-f]{40}$",
          "type": "string"
        },
        "install_mechanism_checkpoint_tag": {
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{2,179}$",
          "type": "string"
        },
        "install_mechanism_checkpoint_target": {
          "pattern": "^[0-9a-f]{40}$",
          "type": "string"
        },
        "installer_receipt_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "main_commit": {
          "pattern": "^[0-9a-f]{40}$",
          "type": "string"
        },
        "packet_commit": {
          "pattern": "^[0-9a-f]{40}$",
          "type": "string"
        },
        "packet_id": {
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{2,179}$",
          "type": "string"
        },
        "pr894_merge": {
          "pattern": "^[0-9a-f]{40}$",
          "type": "string"
        },
        "release_id": {
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{2,179}$",
          "type": "string"
        },
        "runtime_source_commit": {
          "pattern": "^[0-9a-f]{40}$",
          "type": "string"
        },
        "runtime_source_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        }
      },
      "required": [
        "main_commit",
        "pr894_merge",
        "install_checkpoint_tag",
        "install_checkpoint_target",
        "install_mechanism_checkpoint_tag",
        "install_mechanism_checkpoint_target",
        "release_id",
        "packet_id",
        "packet_commit",
        "runtime_source_commit",
        "runtime_source_sha256",
        "installer_receipt_sha256",
        "execution_receipt_sha256",
        "final_seal_receipt_sha256"
      ],
      "type": "object"
    },
    "marker": {
      "const": "VOID_AUTHENTICATED_PAID_WORK_DISABLED_RUNTIME_ACTIVATION_PREREQUISITES_CONFIG_V1"
    },
    "max_receipt_age_seconds": {
      "maximum": 315360000,
      "minimum": 1,
      "type": "integer"
    },
    "version": {
      "const": 1
    }
  },
  "required": [
    "marker",
    "version",
    "enabled",
    "expected",
    "max_receipt_age_seconds"
  ],
  "title": "Authenticated paid-work disabled production runtime activation prerequisites v1",
  "type": "object"
}