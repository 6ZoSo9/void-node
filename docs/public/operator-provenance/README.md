# Operator provenance

This directory contains bounded public receipts for signed operator identity-provenance milestones.

Raw operational receipts, local paths, private signing material, and raw signatures are not committed.
