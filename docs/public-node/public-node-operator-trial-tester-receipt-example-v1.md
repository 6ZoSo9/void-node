# Public Node Operator Trial Tester Receipt Example v1

Status: tester receipt example ready.

Marker: `VOID_PUBLIC_NODE_OPERATOR_TRIAL_TESTER_RECEIPT_EXAMPLE_V1`

Expected proof marker: `VOID_PUBLIC_NODE_OPERATOR_TRIAL_TESTER_RECEIPT_EXAMPLE_V1_GREEN`

## Purpose

Public-safe filled tester receipt example showing how an external tester can report observed public node operator trial results before operator review.

## Boundary

Read-only public example fixture only. This does not enable submission endpoints, wallet sends, money movement, Buy VOID fulfillment, WC issuance, WC ledger writes, WC to VOID swap, validator admission, validator mutation, runtime truth claims, or tester receipts as network truth.

Real tester receipts still require operator review.
