# DataNet WC Held Settlement Final Seal HTML Card Hold v1

Marker: `VOID_DATANET_WC_HELD_SETTLEMENT_FINAL_SEAL_HTML_CARD_HOLD_V1`

## What changed

This brick publishes a browser-visible static HTML card for the held DataNet Work Credits settlement final seal:

- `/public-node/work-credits/datanet-wc-held-settlement-final-seal-html-card-hold-v1.html`

It also publishes a JSON metadata card:

- `/public-node/work-credits/datanet-wc-held-settlement-final-seal-html-card-hold-v1.json`

The card is indexed from:

- `/public-node/work-credits/index.json`

## Boundary

This is HTML-card-only.

Work Credits remain unlimited and uncapped.

Zero WC and zero VOID values are placeholders only.

It does not issue WC.

It does not append a ledger line.

It does not allocate or transfer VOID.

It does not create, sign, or broadcast a transaction.

It does not expose a transaction hash.

It does not create or publish a settlement receipt.

It does not create or publish a verify pack.

It does not access a wallet or signer.

It does not enable a runtime mutation route or mutation handler.

Expected proof result:

`VOID_DATANET_WC_HELD_SETTLEMENT_FINAL_SEAL_HTML_CARD_HOLD_V1_GREEN`
