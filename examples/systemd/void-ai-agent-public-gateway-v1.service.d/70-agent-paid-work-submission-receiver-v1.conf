# Example only. Do not install before the isolated receiver passes a local
# authenticated canary and public route activation is explicitly approved.
[Service]
Environment=VOID_AGENT_PAID_WORK_SUBMISSION_RECEIVER_UPSTREAM=http://127.0.0.1:4187
