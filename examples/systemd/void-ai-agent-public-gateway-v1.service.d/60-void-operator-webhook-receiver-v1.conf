# Example only. Do not install until the receiver service has passed a local
# authenticated canary and the public route activation is explicitly approved.
[Service]
Environment=VOID_OPERATOR_WEBHOOK_RECEIVER_UPSTREAM=http://127.0.0.1:4186
